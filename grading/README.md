# ขาตัดเกรด

โปรแกรมคนละตัวกับตัวเก็บ เดินทีหลัง และรันที่เครื่องบ้าน ไม่ใช่บน Actions เพราะตัวอ่านคือ
`claude-9arm` ที่ต้องผ่านเกตเวย์ที่บ้าน โค้ดอยู่ที่นี่ตามกฎบ้านหลังเดียว ถึงจะไม่ได้รันที่นี่

ตอนนี้มีแค่ **ด่านหนึ่ง** ซึ่งต้องผ่านก่อนลงแรงสร้างบันไดให้ครบทุกวิชาชีพ

## ด่านหนึ่ง — ผู้อ่านคนเดิมนิ่งไหม

ตัดเกรดชุดเดิม 50 งานห้ารอบ สลับตำแหน่งในชุดทุกรอบ แล้วนับว่าแต่ละงานลงขั้นเดิมกี่รอบ
เกณฑ์ของงานหนึ่งชิ้นคือ **≥4/5** ถ้าไม่ผ่าน ทางแก้คือลดขนาดชุดหรือซอยคำถามให้ปิดขึ้น
**ห้ามปรับบันได** เพราะด่านนี้ไม่ได้ถามว่าบันไดถูก — นั่นเป็นคำถามของด่านสองและด่านสาม

```sh
git clone git@github.com:<เจ้าของ>/freelance-market-data.git /tmp/fmdata   # กองข้อมูล
python3 grading/build_sample.py --data-dir /tmp/fmdata --out grading/out
python3 grading/run_gate1.py --out grading/out --dry-run | less            # ดูพรอมป์ก่อนยิง
python3 grading/run_gate1.py --out grading/out                             # ห้ารอบ ~26 นาทีต่อรอบเป็นค่าคาด
python3 grading/score_gate1.py --out grading/out
```

`--reader` ตั้งต้นเป็น `claude-9arm` เปลี่ยนได้ ส่วน `--reader-id` คือชื่อที่จะถูกบันทึกลงทุกแถว
พรอมป์ถูกป้อนทาง stdin ไม่ใช่ทาง argument เพราะทั้งชุดยาวราว 66,000 ตัวอักษร

## ตัวอ่านสองแบบ เลือกให้ตรงกับตัวที่จะตัดเกรดจริง

**`reader_claude.sh` — Claude Code ห่อโมเดลไว้ นี่คือแบบที่ด่านหนึ่งต้องใช้** รูปเดียวกับ
`claude-9arm` คือ harness ใส่พรอมป์ระบบของมันเองเข้าไปและมีลูปเอเจนต์ครอบอยู่ ความนิ่ง
ของโมเดลเปล่ากับความนิ่งของโมเดลที่ถูกห่อไม่ใช่ตัวเดียวกัน วัดผิดตัวก็ตอบคำถามผิดข้อ

```sh
cat > ~/.config/fm-grading.env <<'EOF'
FM_CLAUDE_BASE=https://ปลายทาง        # ระดับราก ไม่ต้องมี /v1 — claude ต่อ /v1/messages ให้เอง
FM_CLAUDE_KEY=คีย์
FM_CLAUDE_MODEL=ชื่อรุ่น
FM_CLAUDE_CONTEXT=128000               # ขนาดหน้าต่างจริง ไม่ใส่ = 128000
EOF
chmod 600 ~/.config/fm-grading.env

python3 grading/run_gate1.py --out grading/out \
    --reader "bash grading/reader_claude.sh" --reader-id <ชื่อรุ่น>
```

ชื่อรุ่นของปลายทางอื่นไม่อยู่ในแค็ตตาล็อกของ `claude` ซึ่ง**ไม่ได้ทำให้เรียกไม่ได้** มันเตือน
แล้วเดินต่อ แต่ถ้าไม่บอก `CLAUDE_CODE_MAX_CONTEXT_TOKENS` มันจะเดาว่าหน้าต่าง 200k
แล้วย่อบทสนทนาตามสมมติฐานที่ผิด ตัวห่อจึงตั้งให้เสมอ เสียงเตือนออกทาง stderr ส่วน stdout
เป็น JSON สะอาดที่ `run_gate1.py` อ่านได้ตรง ๆ

## ยิงตรงเข้า API โดยไม่ผ่าน harness

`reader_api.py` ยิง HTTP เองโดยไม่มี harness ครอบ รองรับทั้งปลายทางแบบ OpenAI และแบบ
Anthropic และเดารูปแบบเองถ้าไม่ได้บอก **ไม่ใช่ตัวที่ใช้ตัดเกรดจริง** จึงใช้กับด่านหนึ่งไม่ได้
ที่ของมันคือด่านสอง ซึ่งถามว่าผู้อ่านคนละคนเห็นตรงกันไหม และปลายทางที่ไม่มี harness
เป็นผู้อ่านอีกคนที่หาได้ถูกที่สุด

```sh
cat > ~/.config/fm-grading.env <<'EOF'
FM_READER_BASE=https://ปลายทาง/v1
FM_READER_MODEL=ชื่อรุ่น
FM_READER_KEY=คีย์
EOF
chmod 600 ~/.config/fm-grading.env

python3 grading/run_gate1.py --out grading/out \
    --reader "python3 grading/reader_api.py" --reader-id <ชื่อรุ่น>
```

**คีย์เข้าทางไฟล์หรือ environment เท่านั้น ไม่เคยเข้าทาง argument** เพราะ argument ของ
กระบวนการอ่านได้จาก `ps` ทั้งเครื่อง และติดไปกับบันทึกของเชลล์ด้วย

**อุณหภูมิเป็นเรื่องใหญ่ของด่านหนึ่ง** ตั้ง 0 แล้วด่านนี้ผ่านฟรี ๆ โดยไม่ได้บอกอะไรเลย ค่าที่ใช้วัด
ต้องเป็นค่าเดียวกับที่จะใช้ตัดเกรดจริง ไม่ใส่ `FM_READER_TEMP` = ใช้ค่าตั้งต้นของปลายทาง
ค่าที่ใช้ถูกบันทึกลงทุกแถว และ `score_gate1.py` ปฏิเสธผลที่ปนหลายอุณหภูมิหรือหลายรูปแบบ API

## อะไรไม่อยู่ใน repo นี้

`grading/out/` ถูก ignore ไว้ทั้งก้อน เพราะมันมีคำอธิบายที่ลูกค้าเขียน ซึ่งเป็นเหตุผลเดียวกับ
ที่กองข้อมูลไปอยู่ repo ส่วนตัว ที่ส่งออกไปให้ตัวอ่านมีแค่คำอธิบายกับแท็ก ไม่มีหมายเลขงาน
ลิงก์ ชื่อผู้ว่าจ้าง หรืองบ อ้างกลับด้วยหมายเลขลำดับในชุดเท่านั้น

## ไฟล์

| ไฟล์ | ทำอะไร |
|---|---|
| `build_sample.py` | ตัดชุดทดสอบ 50 งานจากกองข้อมูล แล้วแช่แข็งไว้เป็นไฟล์เดียว |
| `run_gate1.py` | ยิงห้ารอบ สลับตำแหน่ง เก็บคำตอบดิบและแถวเกรดที่กำกับ rev ของไม้บรรทัดกับรุ่นของตัวอ่าน |
| `score_gate1.py` | นับความครบ ความนิ่ง ผลของตำแหน่งในชุด และสัดส่วนตกขอบ |
| `reader_claude.sh` | ตัวอ่านที่เป็น Claude Code ห่อโมเดลไว้ — แบบที่ด่านหนึ่งต้องใช้ |
| `reader_api.py` | ตัวอ่านที่ยิง HTTP เองโดยไม่มี harness ครอบ สำหรับด่านสอง |
| `ladders/graphic-design.md` | ไม้บรรทัดฉบับร่างสองแกนของกราฟิกดีไซน์ ทุกขั้นยึดด้วยงานจริงยกเว้น K5 |
